import React from 'react';

function JsonGraph () {
    let style = {
        root: {
        },
    };

    return (
        <div style={style.root}>
        </div>
    );
}

export default JsonGraph;
